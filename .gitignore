# Generated by deploy.sh from index.html — never commit
legacy/supabase/functions/outreach-bot/html.ts

# Env / secrets — never commit
.env
.env.*
*.key

.DS_Store

# live data backup for migration (never commit)
live-config.json

# local preview config
.claude/
